{
  "license": "Apache-2.0",
  "scripts": {
    "pre-commit": "pnpm run --filter \"support-*\" pre-commit",
    "pre-push": "pnpm run --filter \"support-*\" pre-push",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}