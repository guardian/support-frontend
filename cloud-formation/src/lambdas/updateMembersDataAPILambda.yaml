UpdateMembersDataAPILambda:
  Type: "AWS::Lambda::Function"
  Description: "Request members data api sync with Zuora/SalesForce"
  Properties:
    $environment_variables
    FunctionName: !Sub ${Stack}-UpdateMembersDataAPILambda-${Stage}
    Handler: "com.gu.support.workers.lambdas.UpdateMembersDataAPI::handleRequest"
    MemorySize: 256
    Role: !GetAtt [ LambdaExecutionRole, Arn ]
    Code:
      S3Bucket: support-workers-dist
      S3Key: !Sub support/${Stage}/monthly-contributions/monthly-contributions.jar
    Runtime: "java8"
    Timeout: "60"