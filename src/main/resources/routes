# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Pinged by load balancer to determine if instance is healthy
GET      /healthcheck                                         controllers.AppController.healthcheck

# Charges a credit/debit card via Stripe
POST     /contribute/one-off/stripe/create                    controllers.StripeController.createCharge
