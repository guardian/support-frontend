describe("sample", () => {
  it("false", () => {
    const result = false;
    expect(result).toEqual(false);
  });
  it("true", () => {
    const result = true;
    expect(result).toEqual(true);
  });
});
