import { type ThemeButton } from '@guardian/source/react-components';

export const observerThemeButton: Partial<ThemeButton> = {
	textPrimary: '#FFFFFF',
	backgroundPrimary: '#C85000',
	backgroundPrimaryHover: '#AF4600',
} as const;
