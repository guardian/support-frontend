import { NewspaperArchiveHeader } from 'components/serverSideRendered/newspaperArchiveHeader';

export default {
	title: 'Server Side Rendered/Newspaper Archive Header',
	component: NewspaperArchiveHeader,
};

export function Default() {
	return <NewspaperArchiveHeader />;
}
