play.application.loader = "wiring.AppLoader"

#### Import private keys
include file("/etc/gu/support-frontend.private.conf")

googleAuth.domain = "guardian.co.uk"

play.filters.csrf.cookie.name = "GU_support_csrf"

paypal.sandbox.url = "https://www.sandbox.paypal.com"